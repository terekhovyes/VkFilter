<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@color/my_white"/>

    <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

        <TextView
                android:id="@+id/title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="15dp"
                android:gravity="center"
                android:text="@string/a_login_app_title"
                android:textSize="72sp"
                android:textColor="@color/my_green"/>

        <ImageView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@id/title"
                android:layout_marginTop="15dp"
                android:adjustViewBounds="true"
                android:src="@drawable/app_picture"/>

        <Button
            android:id="@+id/secondLoginButton"
            android:layout_width="290dp"
            android:layout_height="45dp"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="15dp"
            android:background="@android:color/transparent"
            android:textAllCaps="false"
            android:text="@string/a_login_weblogin_button"
            android:textSize="16sp"
            android:textColor="@color/my_green"/>

        <Button
            android:id="@+id/loginButton"
            android:layout_width="290dp"
            android:layout_height="45dp"
            android:layout_marginBottom="10dp"
            android:layout_above="@id/secondLoginButton"
            android:layout_centerHorizontal="true"
            android:background="@drawable/login_button"
            android:textAllCaps="false"
            android:text="@string/a_login_login_button"
            android:textSize="18sp"
            android:textColor="@color/my_white_new"/>

        <TextView
                android:id="@+id/infoTextView"
                android:layout_width="290dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="35dp"
                android:layout_marginBottom="20dp"
                android:layout_above="@id/loginButton"
                android:layout_centerHorizontal="true"
                android:text="@string/a_login_warning"
                android:textSize="14sp"
                android:gravity="center"
                android:textColor="@color/my_black_tr_new"/>

    </RelativeLayout>

</FrameLayout>
